# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'AppPromo' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for AppPromo

  target 'AppPromoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'AppPromoUITests' do
    # Pods for testing
  end

  pod 'Alamofire'

end
